<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<!-- MyBatis xml 파일 작성 시 반드시 mapper 태그의 namespace 속성값을
     Mapper 인터페이스와 동일한 이름으로 설정해야 합니다. -->
<mapper  namespace="org.zerock.mapper.MemberMapper">

    <!-- read : select * from member where id = 'TestID'  -->
	<select id="read" resultType="org.zerock.domain.MemberVO">
    	select * from member where id = #{id}   
    </select>
    
    <!-- LOGIN -->
    <select id="login" resultType="org.zerock.domain.MemberVO">
    	select * from member where id=#{id} and password=#{password} 
    </select>

	<!-- INSERT 회원가입 -->
	<insert id="insert">
		INSERT INTO member(id, password, email ,name, address1, address2, address3 ) VALUES(#{id},#{password},#{email},#{name},#{address1},#{address2},#{address3})
	</insert>
	
	<!-- UPDATE 회원정보 수정 처리 -->
	<update id="update">
   		UPDATE member SET name = #{name},password = #{password}, email = #{email},  address1 = #{address1}, address2 = #{address2}, address3 = #{address3} WHERE id = #{id}
	</update>

	
	<!-- DELETE 회원계정삭제(탈퇴)처리 -->  
	<!--  delete from member where id='kim1' and password='1234';  -->
	<delete id="deleteMember">
		DELETE FROM MEMBER WHERE id=#{id} and password=#{password}
	</delete>
	
	<!-- FIND ID 회원 아이디 찾기 처리 -->
	<!-- select id from member where name = 'user' and email = '이메일';  -->
	<select id="findId" resultType="org.zerock.domain.MemberVO">
		select id from member where name=#{name} AND email=#{email}
	</select>
	
	<!-- FIND Pwd 회원 비밀번호 찾기 처리 -->
	<select id="findPwd" resultType="org.zerock.domain.MemberVO">
	<!-- select password from member where id='kim1' AND email='kim1@gamil.com'; -->
		select password from member where id=#{id} and email=#{email}
	</select>
</mapper>
